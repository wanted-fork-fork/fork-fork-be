spring:
  application:
    name: forkfork
  profiles:
    include: mongodb, kakao, auth
  servlet:
    multipart:
      enabled: true
      max-file-size: 20MB
      max-request-size: 100MB

link:
  expiration-hours: 24
